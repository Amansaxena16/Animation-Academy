# Used by docker-compose for the Postgres container
POSTGRES_DB=animation_academy
POSTGRES_USER=aa_user
POSTGRES_PASSWORD=change-me

# Host port for Postgres (change if 5432 is taken by a local install)
POSTGRES_PORT=5432
